import Promises from './typescript-examples/promise';
import Functions from './typescript-examples/functions';

const promises = new Promises();
promises.promise0();

const funcs = new Functions();

const a: number = funcs.func3(4);
